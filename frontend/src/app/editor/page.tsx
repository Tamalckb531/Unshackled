import EditorComponent from "@/components/EditorComponent";
import React from "react";

const page = () => {
  //? Time to build the editor

  return (
    <div className=" bg-white h-[89vh] text-4xl text-black text-center border border-red-400">
      <EditorComponent />
    </div>
  );
};

export default page;
